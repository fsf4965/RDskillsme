{
  "name": "appserver",
  "version": "1.0.0",
  "description": "skillsmeuni serverside",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "concurrently \"node server\" \"npm start --prefix webpage\"",
    "heroku-postbuild": "cd webpage && npm install && npm run start"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "multer": "^1.4.3",
    "pg": "^8.6.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.6.5"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
